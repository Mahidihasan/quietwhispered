import React from 'react';

const AuthorNote = () => {
  return (
    <div className="author-note">
      <p className="author-note-text">
        This is a quiet digital footprint.
        A place to leave traces of thoughts, journeys, and questions.
        Not written for crowds, algorithms, or approval —
        but as a record of becoming.
        Outside the noise, outside the chaos,
        this space exists simply to remember, reflect,
        and stay honest with time.
      </p>
    </div>
  );
};

export default AuthorNote;
